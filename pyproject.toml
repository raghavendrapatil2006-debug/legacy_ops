[project]
name = "legacy-ops"
version = "1.0.0"
description = "CyberQA Legacy Ops Environment"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"